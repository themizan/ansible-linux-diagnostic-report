---
sudo: required
language: python
python: 
  - "2.7"
  - "3.7"
services:
  - docker
before_install:
  - sudo apt-get -qq update
install:
  - pip install -r requirements.txt
script:
  - molecule test